# Hermes Agent Evals v0.2 — Portability + Longitudinal Canary

**Date:** 2026-08-20  
**Harness SHA:** `e7b6caa11755e795d98d9c37807c4006a497fed2`  
**v0.1 tag:** `v0.1.0` → `0641093d26fe0fa7c91a771029dd61441a06ff6a`  
**Research lane only.** No Hermes core edits. No NousResearch/hermes-agent writes.

This pass proves PORTABILITY + MEASUREMENT VALIDITY + LONGITUDINAL USEFULNESS
before breadth. No new Hermes bug fixtures.

---

## 1. Release freeze

| Item | Value |
|---|---|
| v0.1 tag | `v0.1.0` (annotated) |
| v0.1 release SHA | `0641093d26fe0fa7c91a771029dd61441a06ff6a` |
| v0.1 tag state at freeze | clean (tag points at that commit) |
| v0.2 commit | `e7b6caa11755e795d98d9c37807c4006a497fed2` |
| v0.2 tree at historical run | clean (`harness_dirty=false`) |
| Historical 3/3 | **PASSED** (clean clone; see §2) |

Fixture SHA-256 digests (`python -m hermes_eval freeze`):

| Path | sha256 | bytes |
|---|---|---|
| evals/fixtures/zero-toolset.yaml | `6b4aeda1ffb0018b3ead42ad5727da1f21e0be639844c9df9e20df646bd186b4` | 1566 |
| evals/fixtures/delegate-fallback-runtime.yaml | `905f313f3666517c2526c4c5602452220d192c0e15256032290825dd5addb2ef` | 2243 |
| evals/fixtures/stale-pin-rescope.yaml | `3d677aefa9b1ea45e144bd06db6f0f779858cedeadef7718a0718b8355c3e666` | 1240 |
| evals/fixtures/zero-toolset-live.yaml | `0d6dec80a031138ca8e09bcdc6e2475fe544f6a4886046774e199bfcce2f25c6` | 1225 |
| evals/fixtures/compression-prefix-probe.yaml | `441afd7d90d8370df535ee99d120c48fc3d39793c43de219f636d630521677a3` | 729 |
| evals/provenance/expected-historical.json | `f332daede6cd3ed51e0177129bd5ed5e4c99479fd43e8054ca12df00e3130d85` | 1106 |
| evals/provenance/manifest.json | `dcc89914fd25bcaf44d4c9164e0a2ab6d571756e2e1c61441adde25520872315` | 3765 |
| evals/suites/core-failures.yaml | `42d8d7102751b9e15ca1735978530c64c4c0e1e51a4b4ea9dffd9fd2b5358824` | 125 |

Classifications of the historical three: `agent_behavior` (zero-toolset),
`fault_injected_invariant` (delegate-fallback-runtime), `state_transition`
(stale-pin-rescope). None is `production_replay`.

---

## 2. Clean-clone proof

Fresh environment: `C:\Users\jmcke\AppData\Local\Temp\hae-v02-clone`  
(`git clone` of this repo, detached at `e7b6caa`). Not a copy of
`C:\dev\hermes-agent-wt-*`.

Interpreter honesty: the clone's default `python` is not required to import
Hermes deps. This run set:

```
HERMES_EVAL_PYTHON=c:\dev\hermes-agent\.venv\Scripts\python.exe
PYTHONPATH=<clone>
```

Isolation is the **SUT checkout path**, not a second venv. `PYTHONPATH`
included the clone first, then the fetched worktree — never
`C:\dev\hermes-agent-wt-*`.

Commands:

```
git clone <hermes-agent-evals> %TEMP%\hae-v02-clone
git -C %TEMP%\hae-v02-clone checkout --detach e7b6caa11755e795d98d9c37807c4006a497fed2
set PYTHONPATH=%TEMP%\hae-v02-clone
set HERMES_EVAL_PYTHON=c:\dev\hermes-agent\.venv\Scripts\python.exe
python -m hermes_eval fetch-sut
python -m hermes_eval compare --historical --suite core-failures
```

Fetched SUT SHAs (GitHub → clone `.cache/hermes-sut`):

- `13ce0c5c675e843af70d19c9e5144249cd51c8d1`
- `ed5b9152ce975ada68f0b53a21c4806f29ed0852`
- `c6a2fb48af74e3c795015aeb6e615733a9b5bac5`
- `623b93200779d31d416eb2c5f9116106de6f5adb`

Remotes: `https://github.com/NousResearch/hermes-agent.git`,
fallback `https://github.com/trippyogi/hermes-agent.git`.

**Historical gate: PASSED 3/3** — every fixture `candidate_fixed` and
identical to `evals/provenance/expected-historical.json`.

`hermes_root` in every result JSON is under the **clone**:

| Arm | hermes_root |
|---|---|
| zero-toolset bad | `...\hae-v02-clone\.worktrees\13ce0c5c675e` |
| zero-toolset good | `...\hae-v02-clone\.worktrees\ed5b9152ce97` |
| delegate bad | `...\hae-v02-clone\.worktrees\13ce0c5c675e` |
| delegate good | `...\hae-v02-clone\.worktrees\c6a2fb48af74` |
| pin bad | `...\hae-v02-clone\.worktrees\13ce0c5c675e` |
| pin good | `...\hae-v02-clone\.worktrees\623b93200779` |

No `C:\dev\hermes-agent-wt-*`. Manifest remotes stay GitHub; operator
filesystem paths are not required.

---

## 3. Current canary

Resolved current Hermes SHA by fetching `refs/heads/main` (full SHA stored,
never the label): `fab8479aa005e3c6f9e779a2b9da4e186a17db4a`.

| Fixture | Resolved current Hermes SHA | Status | Reason |
|---|---|---|---|
| zero-toolset | `fab8479aa005e3c6f9e779a2b9da4e186a17db4a` | FIX_NOT_ON_THIS_SHA | known_good `ed5b9152…` is not an ancestor; fixture failed. Expected until that fix (or equivalent) lands. Not a regression vs current main. |
| delegate-fallback-runtime | `fab8479aa005e3c6f9e779a2b9da4e186a17db4a` | FIX_NOT_ON_THIS_SHA | known_good `c6a2fb48…` is not an ancestor; fixture failed. Not a regression. |
| stale-pin-rescope | `fab8479aa005e3c6f9e779a2b9da4e186a17db4a` | FIX_NOT_ON_THIS_SHA | known_good `623b9320…` is not an ancestor; fixture failed. Not a regression. |

Ancestry was re-checked after deepening the SUT cache to **19,809** commits
of current main. `merge-base --is-ancestor` is still false for all three
known_good SHAs. Their merge-bases with current are their **parents**
(`87e32b6b`, `49cc3708`, `65755071`) — the replay bases — not the fix
commits. Those exact SHAs have not landed on `origin/main`.

**No REGRESSION claims.** `REGRESSION` is only legal when known_good is an
ancestor **and** the fixture fails.

---

## 4. Prefix experiment

v0.1 called `run_conversation` separately (`message_count=2` each). v0.2
wraps `_build_api_kwargs` / `_interruptible_api_call` and **accumulates**
one session: T1–T4 same system/tools with suffix growth; T5 force
`compress()`. SHA `13ce0c5c675e843af70d19c9e5144249cd51c8d1`.
`hermes_root` = clone `.worktrees\13ce0c5c675e`.

| turn | system hash | tools hash | message count | shared prefix vs T1 | prefix retention | compression event | first divergence |
|---|---|---|---|---|---|---|---|
| T1 | `54ce7e5400b938bc` | `76a7d12a8aadd424` | 2 | 2 | 1.0 | no | — |
| T2 | `54ce7e5400b938bc` | `76a7d12a8aadd424` | 4 | 2 | 1.0 | no | — |
| T3 | `54ce7e5400b938bc` | `76a7d12a8aadd424` | 6 | 2 | 1.0 | no | — |
| T4 | `54ce7e5400b938bc` | `76a7d12a8aadd424` | 8 | 2 | 1.0 | no | — |
| T5 | `111dac948f37befd` | `76a7d12a8aadd424` | 12 | 0 | 0.0 | yes | 0 |

T1–T4 keep system + tools hashes and the full T1 prefix (retention 1.0)
while message count grows 2→8. T5 `compress()` changes the system hash and
drops prefix retention to 0 (first divergence at index 0). Provider
`cache_read` remains `not_observable` on the mock.

---

## 5. Waste episodes

Detector hits: **50**  
Unique episodes (source, event index, tool): **38**  
Overlaps collapsed: **11** (W1+W3 on the same retry is one human decision;
one episode is W1+W3+W2)

Hit labels: W1=8, W2=6, W3=19, W4=7, W5=3, W6=7

Human-label artifact: `reports/evals/wasted-turn-labeling-episodes.md`  
Machine JSON (clone run): `results/wasted-turn-scan.json` (gitignored)

`REAL_ATOF_DATA`: **BLOCKED** (`HERMES_EVAL_ATOF_DIR` unset; no ATOF jsonl).
Corpus remains **scrubbed reconstruction**. No auto-score. Do not label the
50 hit rows as independent.

---

## 6. Live behavior

**BLOCKED.** Process env lacked `HERMES_EVAL_API_KEY`,
`HERMES_EVAL_PROVIDER`, and `HERMES_EVAL_MODEL`. Runner did not read
`~/.hermes`. No synthetic rates.

| Field | Value |
|---|---|
| status | BLOCKED |
| model / provider | null / null |
| repetitions | not started |
| control_task_success_rate | not invented |
| fault_task_success_rate | not invented (expect ~0 when RUN) |
| fault_textual_pseudo_tool_call_rate | not invented |
| fault_diagnostic_rate | not invented |

Scoring change (when credentials exist): report those four rates
separately. Known-good makes zero tools **loud**; it does **not** restore
tools. Fault-arm task success must not be treated as evidence the salvage
commit fixed the task.

---

## 7. Readiness

| Suite | Status | Blockers |
|---|---|---|
| Deterministic regression canary | **READY** | None for the three historical fixtures. Clean-clone 3/3 + portable GitHub fetch + ancestry-aware current canary. Current main is `FIX_NOT_ON_THIS_SHA` until those fix SHAs (or equivalents) land. |
| Agent-behavior suite | **NOT READY** | Live matrix BLOCKED without `HERMES_EVAL_*`. No real ATOF (`REAL_ATOF_DATA=BLOCKED`). Prefix probe is instrumentation, not a pass/fail SUT gate. Waste episodes are unlabeled scrubbed reconstructions. |

---

## 8. Next experiments

Only after the gates above. Ranked by information gain:

1. **Live weak-model zero-toolset (RUN, not BLOCKED).** Set
   `HERMES_EVAL_API_KEY` / `PROVIDER` / `MODEL` in the process env. Measure
   `fault_textual_pseudo_tool_call_rate` and `fault_diagnostic_rate` on a
   real model. Expect `fault_task_success_rate ≈ 0`. Highest gain: this is
   the only check that the salvage is *loud* rather than *task-fixing*.
2. **Real ATOF waste labeling on episodes.** Point
   `HERMES_EVAL_ATOF_DIR` at production ATOF jsonl. Label the 38-style
   episode sheet (not 50 hits). Highest gain for taxonomy validity.
3. **Re-canary after a fix SHA is an ancestor of `origin/main`.** Then
   `PASS` / `REGRESSION` become legal. Until then, current-main failures
   stay `FIX_NOT_ON_THIS_SHA`.

Do not add new Hermes bug fixtures before those gates.
